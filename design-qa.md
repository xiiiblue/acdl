# 设计QA报告

## 对照基线

- 视觉真值：`/Users/bluexiii/.codex/generated_images/01a031bf-5aa1-7e82-9429-8727f5c6488c/exec-8cb07585-3ceb-47cc-b7da-7ae0a408e195.png`
- 实现截图：`/Users/bluexiii/code/public/hive-acdl-site/qa/implementation-home-light.png`
- 并排对照：`/Users/bluexiii/code/public/hive-acdl-site/qa/design-comparison-home.png`
- 页面状态：首页、浅色主题、动画完成后的静止状态
- CSS视口：`1502×1069`
- 源图像素：`1487×1058`
- 实现图像素：`1487×1058`
- 密度归一化：两张图均按原始像素1:1并排，没有缩放或拉伸

## 全屏对照结论

实现保留了方案2的核心构图：紧凑顶栏、左对齐编辑型首屏、四阶段生命周期横线和紧随其后的章节目录。首屏在同一高度内显示四个阶段和四条完整章节记录，信息密度、主次关系和留白比例与视觉真值一致。

## 五项保真检查

- 字体与排版：实现使用博客同款Inter与系统中文无衬线字体栈；标题采用高字重、紧字距和清晰的三级层级。正文与小型界面文字保持可读，没有裁切或异常换行。
- 间距与布局节奏：页面宽度、首屏高度、生命周期节点间距和章节行高已按同尺寸截图校正。桌面端没有横向溢出，移动端`390×844`检查通过。
- 颜色与视觉变量：浅色与深色主题直接复用博客的`#f7faff`、`#1f2f49`、`#3979e8`、`#101827`和`#78a8ff`等主题变量，边界与表面层级一致。
- 图像与资产：所选方案不含照片或插画。月亮、太阳和箭头使用Phosphor图标库，没有使用占位符、Emoji或手工绘制图标。
- 文案与内容：首页保留ACDL的真实标题、目标和15章导航；阅读页保留完整Markdown内容、搜索、目录及前后章导航。

## 聚焦区域检查

并排图中首屏标题、主按钮、生命周期节点和前四条章节记录均可直接阅读，因此无需额外裁剪。阅读页另以`qa/implementation-reader-light.png`检查标题、正文、左右导航和搜索框；移动端以`qa/implementation-home-mobile.png`和`qa/implementation-reader-mobile.png`检查响应式结构。

## 交互与可访问性

- 已验证浅色与深色主题切换，并保存用户选择。
- 已验证“从第一章开始”进入第一章。
- 已验证站内搜索，查询“版本”返回6条结果。
- 已验证阅读进度条随滚动更新。
- 已验证桌面端、移动端、键盘焦点样式和`prefers-reduced-motion`降级规则。
- 最终浏览器控制台没有警告或错误。

## 对照迭代历史

### 第一轮

- [P2]首页右侧的巨大ACDL字样更接近方案1，改变了方案2以留白突出主流程的构图。
- [P2]额外的“阅读指南”大标题把章节目录推到首屏以下，同尺寸视口只能看到章节标题，看不到完整记录。
- 修复：删除右侧装饰字样和额外章节标题，压缩首屏与生命周期的垂直间距，把章节目录直接接在生命周期之后。

### 第二轮

- 对照证据：`qa/design-comparison-home.png`
- 结果：首屏可见四个生命周期阶段和四条完整章节记录；没有遗留P0、P1或P2问题。

### 第三轮用户反馈

- 反馈证据：用户提供的线上内容页截图，重点标注顶部标题栏。
- 首页：统一内容最大宽度由`1340px`收窄至`1200px`，保留移动端原有边距和响应式断点。
- 内容页：标题栏背景和底部分隔线改为全视口贯通，内部品牌与操作区使用独立容器对齐阅读三栏，不再被重复的视口边距挤向中间。
- 回归结果：桌面端标题栏与阅读区对齐；移动端`390×844`无横向滚动，搜索、章节导航和主题切换布局正常。

### 第四轮用户反馈

- 视觉真值：`qa/reference-lifecycle-line.png`，原始像素`2500×370`。
- 实现截图：`qa/implementation-lifecycle-v3.png`，浏览器截图像素`1265×712`，CSS视口`1280×900`，浅色主题、入场动画完成状态。
- 并排证据：`qa/design-comparison-lifecycle-v3.png`。源图等比例归一化为`1265×187`，实现裁取同宽生命周期区域`1265×195`，两者没有拉伸。
- [P2]原实现的连接线穿过“认知、设计、实施、验证”标题。修复后标题使用页面背景层遮挡线段，线条只在节点与标题间可见，文字笔画保持完整清晰。
- 四个阶段已从静态展示改成语义化链接，分别进入第01、05、08、10章；真实点击逐项验证通过。
- 移动端`390×844`保持单列阶段布局，没有横向溢出；浏览器控制台没有错误。
- 聚焦区域无需另做裁切：并排证据本身就是生命周期区域的近距离对照，四个标题和连接线均可直接辨认。
- 五项保真检查：字体、间距、主题色和真实文案保持原方案；该区域没有图像资产，节点和线条沿用既有界面样式。
- 修复后没有遗留P0、P1或P2问题。

### 第五轮交互修正

- 按用户反馈取消四个阶段到内容页的跳转，改为首页目录锚点。
- 四个阶段分别定位到目录中的第01、05、08、10章，页面路径保持在`/acdl/`。
- 浏览器真实点击验证：四个锚点均将目标目录项定位到距视口顶部约`24px`的位置。
- 目标章节使用主题浅蓝色背景标记；键盘语义仍为普通链接，保留焦点反馈。

## 后续微调

- [P3]实现的顶栏只保留“手册”和“博客”，比视觉真值更克制。这是为了避免把博客的归档、标签和RSS误表现为ACDL站内功能。
- [P3]章节摘要使用真实手册元数据，因此部分行的文字长度与生成图不同，但不影响结构保真。

final result: passed
